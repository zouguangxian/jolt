[package]
name = "revm-host"
version = "0.1.0"
edition = "2021"

[dependencies]
jolt-sdk = { path = "../../jolt-sdk", features = ["host"] }
guest = { package = "revm-guest", path = "./guest" }

[features]
icicle = ["jolt-sdk/icicle"]
